# vim:foldmethod=marker:foldlevel=0

#
#
# ~/.config/yazi/yazi.toml
#
#

[mgr]     # {{{
ratio          = [2, 8, 7]
sort_by        = "natural"
sort_sensitive = false # Case sensitive sort
sort_reverse   = false
sort_dir_first = true
sort_translit  = true  # Transliterate filename letters with fancy accents for natural sort
                       # (i.e. replace Â with A, Æ with AE, etc.)
sort_fallback  = "alphabetical"
linemode       = "none"
show_hidden    = true
show_symlink   = true
scrolloff      = 5
mouse_events   = [ "click" ] # Other options like "drag" and "scroll" exist
# }}}

[preview] # {{{
wrap            = "no"
tab_size        = 4
max_width       = 900 # < These are also used for preloading and caching
max_height      = 600 # <
cache_dir       = "~/.local/cache/yazi"  
image_delay     = 0  # alleviate potential lag caused by scrolling file list quickly.
image_filter    = "triangle"
image_quality   = 75
ueberzug_scale  = 1
ueberzug_offset = [ 0, 0, 0, 0 ]
# }}}

[opener]  # {{{
open = [ { run = "xdg-open %s1",   desc = "Open", orphan = true, }, ]
exif = [ { run = "clear; exiftool %s1; echo 'Press enter to exit'; read _", desc = "Show EXIF", } ]
copy = [ { run = "wl-copy < %s1",  desc = "Copy File to Clipboard", } ]

text = [
    { run = "nvim %s",             desc = "Neovim",                  block = true },
    { run = "nvim -R %s",          desc = "Neovim (Read Only)",      block = true },
    { run = "cat -s %s | wl-copy", desc = "Copy Text to Clipboard", },
]

video = [ { run = "mpv %s",    desc = "Play",    orphan = true, }, ]
image = [ { run = "imv -r %s", desc = "Gallery", orphan = true, } ]
audio = [ { run = "wezterm start --always-new-process mpv --no-video %s", desc = "Play", orphan = true }, ]
media = [ { run = "mediainfo %s1; echo 'Press enter to exit'; read _", block = true, desc = "Show mediainfo" }, ]

extract   = [ { run = "ya pub extract --list %s", desc = "Extract here" }, ]
download  = [
    { run = "ya emit download %S",        desc = "Download" },
    { run = "ya emit download --open %S", desc = "Download and open" },
]

noop = [ { run = ":", desc = "Nothing" } ]
# }}}

[open]    # {{{
rules = [
    # Folder
    { url = "*/", use = [ "noop", "image", ] },
    # Text
    { mime = "text/*",      use = [ "text", "exif" ] },
    { mime = "inode/empty", use = [ "text", ] },
    { mime = "application/{json,ndjson,javascript,wine-extension-ini}", use = [ "text", "exif" ] },
    # Media
    { mime = "video/*", use = [ "video", "copy", "media", "exif" ] },
    { mime = "audio/*", use = [ "audio", "copy", "media", "exif" ] },
    { mime = "image/*", use = [ "image", "copy", "media", "exif" ] },
    # Archive
    { mime = "application/{zip,rar,7z*,tar,gzip,xz,zstd,bzip*,lzma,compress,archive,cpio,arj,xar,ms-cab*}", use = [ "extract", "exif" ] },
    # Virtual file system
    { mime = "vfs/{absent,stale}", use = "download" },
    # Fallback
    { url = "*", use = [ "open", "text", "copy", "exif", ] },
]
# }}}

[tasks]   # {{{
file_workers     = 3
plugin_workers   = 5
fetch_workers    = 5
preload_workers  = 5
process_workers  = 5
bizarre_retry    = 3
image_alloc      = 536870912  # 512MB
image_bound      = [ 10000, 10000 ]
suppress_preload = false
# }}}

[plugin]  # {{{
fetchers = [   # {{{
    # MIME-type
    { url = "*/",         run = "mime.dir",    prio = "high", group = "mime" },
    { url = "local://*",  run = "mime.local",  prio = "high", group = "mime" },
    { url = "remote://*", run = "mime.remote", prio = "high", group = "mime" },
] # }}}
spotters = [   # {{{
    # Multi-file
    { mime = "multi/*", run = "multi" },
    # Folder
    { url = "*/", run = "folder" },
    # Code
    { mime = "text/*", run = "code" },
    { mime = "application/{mbox,javascript,wine-extension-ini}", run = "code" },
    # Image
    { mime = "image/{avif,hei?,jxl}", run = "magick" },
    { mime = "image/svg+xml", run = "svg" },
    { mime = "image/*", run = "image" },
    # Video
    { mime = "video/*", run = "video" },
    # Virtual file system
    { mime = "vfs/*", run = "vfs" },
    # Error
    { mime = "null/*", run = "null" },
    # Fallback
    { url = "*", run = "file" },
] # }}}
preloaders = [ # {{{
    # boydaihungst/mediainfo
    { mime = "{audio,video,image}/*", run = "mediainfo" },
    { mime = "application/subrip",    run = "mediainfo" },

    # kirasok/torrent-preview
    { mime = "application/bittorrent", run = "torrent-preview" },

    # alberti42/faster-piper Disabled {{{!CLOSE
    #{ mime = "application/sqlite3", run = 'faster-piper -- sqlite3 "$1" ".schema --indent"' },
    #{ url = "*.md",     run = 'piper -- CLICOLOR_FORCE=1 glow -w=$w -- "$1"' },
    #{ url = "*.tar*",   run = 'piper --format=url -- tar tf "$1"' },
    #{ url = "*.txt.gz", run = 'piper -- gzip -dc "$1"' },
    #{ url = "*.csv",    run = 'piper -- bat -p --color=always --theme=ansi "$1"' },
    #{ url = "*/",       run = 'piper -- eza -TL=2 --color=always --icons=always --group-directories-first --no-quotes "$1"' },
    # }}}

    # PDF
    { mime = "application/pdf", run = "pdf" },
    # Font
    { mime = "font/*", run = "font" },
    { mime = "application/ms-opentype", run = "font" },
] # }}}
previewers = [ # {{{
    # boydaihungst/mediainfo
    { mime = "{image}/*",          run = "mediainfo --no-metadata" },
    { mime = "{audio,video}/*",    run = "mediainfo" },
    { mime = "application/subrip", run = "mediainfo" },
  
    # kirasock/torrent-preview
    { mime = "application/bittorrent", run = "torrent-preview" },
  
    # alberti42/faster-piper Disabled {{{!CLOSE
    #{ mime = "application/sqlite3", run = 'faster-piper --rely-on-preloader' },
    #{ url = "*.md",     run = 'faster-piper --rely-on-preloader' },
    #{ url = "*.tar*",   run = 'faster-piper --rely-on-preloader --format=url' },
    #{ url = "*.txt.gz", run = 'faster-piper --rely-on-preloader' },
    #{ url = "*.csv",    run = 'faster-piper --rely-on-preloader' },
    #{ url = "*/",       run = 'faster-piper --rely-on-preloader' },
    # }}}
    # yazi-rs/plugin:piper
    { url = "*.md",     run = 'piper -- CLICOLOR_FORCE=1 glow -w=$w -- "$1"' },
    { url = "*.tar*",   run = 'piper --format=url -- tar tf "$1"' },
    { url = "*.txt.gz", run = 'piper -- gzip -dc "$1"' },
    #{ url = "*/",       run = 'piper -- eza -TL=2 --color=always --icons=always --group-directories-first --no-quotes "$1"' },

    # peterfication/mux
    { url = "*/", run = "mux eza_tree_1 eza_tree_2 eza_tree_3" },
    #{ url = "*/", run = "mux eza_tree_1 eza_tree_2 eza_tree_3 eza_tree_size_1 eza_tree_size_2 eza_tree_size_3" },


    # Code
    { mime = "text/*", run = 'piper -- bat --plain --color=always "$1"' },
    { mime = "application/{mbox,javascript,wine-extension-ini}", run = "code" },
    # JSON
    { mime = "application/{json,ndjson}", run = "json" },
    # PDF
    { mime = "application/pdf", run = "pdf" },
    # Archive
    { mime = "application/{zip,rar,7z*,tar,gzip,xz,zstd,bzip*,lzma,compress,archive,cpio,arj,xar,ms-cab*}", run = "archive" },
    { mime = "application/{debian*-package,redhat-package-manager,rpm,android.package-archive}", run = "archive" },
    { url = "*.{AppImage,appimage}", run = "archive" },
    # Virtual Disk / Disk Image
    { mime = "application/{iso9660-image,qemu-disk,ms-wim,apple-diskimage}", run = "archive" },
    { mime = "application/virtualbox-{vhd,vhdx}", run = "archive" },
    { url = "*.{img,fat,ext,ext2,ext3,ext4,squashfs,ntfs,hfs,hfsx}", run = "archive" },
    # Font
    { mime = "font/*", run = "font" },
    { mime = "application/ms-opentype", run = "font" },
    # Empty file
    { mime = "inode/empty", run = "empty" },
    # Virtual file system
    { mime = "vfs/*", run = "vfs" },
    # Error
    { mime = "null/*", run = "null" },
    # Fallback
    { url = "*", run = "file" },
] # }}}
# }}}

[input]   # {{{
cursor_blink = false

# cd     {{{
cd_title  = "Change directory:"
cd_origin = "top-center"
cd_offset = [ 0, 2, 50, 3 ]
# }}}

# Create {{{
create_title  = [ "Create:", "Create (dir):" ]
create_origin = "top-center"
create_offset = [ 0, 2, 50, 3 ]
# }}}

# Rename {{{
rename_title  = "Rename:"
rename_origin = "hovered"
rename_offset = [ 0, 1, 50, 3 ]
# }}}

# Filter {{{
filter_title  = "Filter:"
filter_origin = "top-center"
filter_offset = [ 0, 2, 50, 3 ]
# }}}

# Find   {{{
find_title  = [ "Find next:", "Find previous:" ]
find_origin = "top-center"
find_offset = [ 0, 2, 50, 3 ]
# }}}

# Search {{{
search_title  = "Search via {n}:"
search_origin = "top-center"
search_offset = [ 0, 2, 50, 3 ]
# }}}

# Shell  {{{
shell_title  = [ "Shell:", "Shell (block):" ]
shell_origin = "top-center"
shell_offset = [ 0, 2, 50, 3 ]
# }}}
# }}}

[confirm] # {{{
# Trash {{{
trash_title  = "Trash {n} selected file{s}?"
trash_origin = "center"
trash_offset = [ 0, 0, 70, 20 ]
# }}}

# Delete {{{
delete_title  = "Permanently delete {n} selected file{s}?"
delete_origin = "center"
delete_offset = [ 0, 0, 70, 20 ]
# }}}

# Overwrite {{{
overwrite_title  = "Overwrite file?"
overwrite_body   = "Will overwrite the following file:"
overwrite_origin = "center"
overwrite_offset = [ 0, 0, 50, 15 ]
# }}}

# Quit {{{
quit_title  = "Quit?"
quit_body   = "There are unfinished tasks, quit anyway?\n(Open task manager with default key 'w')"
quit_origin = "center"
quit_offset = [ 0, 0, 50, 15 ]
# }}}
# }}}

[pick]    # {{{
open_title  = "Open with:"
open_origin = "hovered"
open_offset = [ 0, 1, 50, 7 ]
# }}}

[which]   # {{{
sort_by        = "none"
sort_sensitive = false
sort_reverse   = false
sort_translit  = false
# }}}
